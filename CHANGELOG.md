# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/) 规范。

---

## [0.3.10] - 2026-08-27

### 新增
- ✨ 数据导入新增"其他格式"标签页：基金购买交易 CSV 导入
  - 按 `hs_code` 匹配基金账户别名（`account_other_name`）生成「银行 → 基金」转账交易
  - 记录份额（`unit`）与单价（`purchase_unit_cost`），`purchase_bank` 模糊匹配转出账户机构
  - 上传后预览：自动匹配的行直接导入，未匹配（如银行名英文 vs 中文）的行可手动选择基金/银行账户或跳过，也支持"仅导入已匹配"
  - 确认导入时可选择转账交易类别（下拉列出 Category 表中的转账分类，与添加交易页一致；无转账分类时自动创建默认"账户转账"）
  - 自动去重（同基金账户同日同金额），重复导入自动跳过
  - CLD 交易导入入口已预留（待实现）
- ✨ 转账交易编辑优化：点击任一侧编辑时同时显示转出与转入两条交易
  - 详情预览同时展示转出/转入账户与金额，基金一侧显示份额与单价
  - 编辑弹窗分「转出」「转入」两栏，可分别修改账户、金额、货币及单位/单价（联动自动计算）
  - 列表查询预加载配对交易，避免 N+1 查询

### 变更
- 🔧 确认导入时，未手动处理且无法自动匹配的行计为「跳过（未处理）」而非失败；仅数据有误或手动指定后仍无法解析的行计为失败
- 🔧 导入结果提示细分跳过原因（已存在 / 未处理 / 用户跳过）

### 修复
- 🐛 修复编辑转账时两侧账户被设为同一账户的问题（转出/转入可独立修改）

### 测试
- ✅ 新增 `test_import_fund_purchase.py`（12 个测试用例：基金购买导入/手动映射/去重/跳过/类别/路由/未处理跳过）
- ✅ 新增转账编辑测试（`test_edit_transfer_*`：两侧账户/金额/基金份额单价更新、缺失校验、列表双侧数据）

---

## [0.3.9] - 2026-08-26

### 新增
- ✨ 定期存款（TIME_DEPOSIT）/ 货币联系存款（CURRENCY_LINKED_DEPOSIT）功能支持
  - 转账到存款账户后弹出"开立定存/CLD"填写窗口，交易内容（货币、金额、开立日期）已预填
    - 填写年利率、到期日期即可建立存款记录，自动关联开立交易（`trans_deposit_id`）
    - CLD 账户额外提供挂钩货币、挂钩货币金额、触发汇率栏位
    - 校验：金额、年利率、开立/到期日期必填，到期日期需晚于开立日期
  - 转账从存款账户转出（到期）后弹出"到期登记"窗口
    - 下拉选择该账户未到期存款，选中后自动回填本金、利率、期限等信息
    - 到期收回金额预填为转账金额，以 HKD 到期时自动计算已实现收益（收回金额 - 本金）
  - 校验失败时保留已填内容重新弹出窗口
- ✨ 定期存款列表页（`/deposits`）：查看所有存款（状态/货币/本金/利率/期限/到期收回/已实现收益）
  - 支持手动开立存款（不关联交易）
- ✨ 导航栏新增"定期存款"入口

### 变更
- 🔧 `TimeDeposit` 表新增 `account_id` 字段，关联存款账户
- 🔧 到期时自动计算 `realized_pnl`（仅以 HKD 基础货币到期适用），`matured_currency_name` 记录到期货币
- 🔧 启动时自动迁移：`time_deposit` 表补齐 `account_id` 列

### 数据库
- 🗄️ `time_deposit` 表新增 `account_id` 字段

### 测试
- ✅ 新增 `test_deposits.py`（10 个测试用例：开立/到期/校验/列表）


---

## [0.3.8] - 2026-08-24

### 新增
- ✨ 账户新增"单位概念"标记（`account_has_unit_ind`），基金账户默认开启
  - 新增/编辑账户时可勾选"有单位概念"（按单位/单价记账，如基金份额）
  - 账户列表类型列显示"单位"徽标，标识按单位记账的账户
- ✨ 账户新增 ISIN 代码字段（`account_isin`，12 位）
  - 基金等有价证券账户可记录证券/基金唯一标识（如 HK0000064689），账户列表名称下显示
  - 新增账户表单按基金类型自动显隐 ISIN 栏位，输入自动转大写
- ✨ 基金/单位账户交易支持录入单位与单价：单位 × 单价 = 账户默认货币金额
  - 外币基金账户（如 USD）单价以账户货币计，金额自动按汇率换算 HKD
  - 填写任意两项（金额、单位、单价）自动补全第三项，三项均填时校验单位 × 单价与金额相符
- ✨ 编辑交易支持修改基金账户的单位/单价（编辑弹窗新增单位字段，联动自动计算）
- ✨ 外币基金账户日终余额（`get_account_eod_balance`）按份额 × 单价追踪余额/持仓成本

### 变更
- 🔧 外汇与投资单位字段拆分存储，互不混淆
  - 外汇交易：`trans_fx_currency_name` / `trans_fx_rate` / `trans_fx_amount`
  - 投资单位（基金等）：`trans_unit` / `trans_unit_price` / `trans_unit_name`
  - `trans_is_rhs_currency_ind`：外汇交易时必填，`trans_currency_name` 为汇率对 RHS 货币时为 True（如 USD/HKD 的 HKD）
  - 移除 `trans_unit_is_fx_ind` 标记字段
- 🔧 仪表盘单位字段显隐改为按账户 `account_has_unit_ind` 判断（替代按账户类型 FUND 判断）
- 🔧 CSV 导入：外汇交易写入 `trans_fx_*` 及 `trans_is_rhs_currency_ind`，单位列仅对非外汇交易解析
- 🔧 启动时自动迁移：补齐缺失列 → 迁移旧数据 → 尝试删除过时列，兼容旧版本数据库

### 修复
- 🐛 修复添加基金交易时无法填写单位/单位单价（表单账户下拉框补充 `data-has-unit`）
- 🐛 修复转账时转出户口为基金户口不显示单位/单位单价填写位置
- 🐛 修复编辑交易时单位/外汇标记字段未随账户变更正确清理

### 数据库
- 🗄️ Account 表新增 `account_has_unit_ind` 字段（基金账户默认为 True）
- 🗄️ Account 表新增 `account_isin` 字段（基金等有价证券 ISIN 代码）
- 🗄️ Transaction 表外汇（`trans_fx_*`）与投资单位（`trans_unit_*`）字段拆分存储，新增 `trans_unit_name`，删除 `trans_unit_is_fx_ind`

---

## [0.3.7] - 2026-06-29

### 新增
- ✨ 交易表单新增单位（unit）/ 单位单价（unit price）可选字段
  - 适用于基金账户（FUND）及所有外币账户（currency ≠ HKD）、非转账交易
  - 自动计算：填写任意两项（金额、单位、单位单价），自动补全第三项
  - 表单校验：三项均填写时，若金额 ≠ 单位 × 单位单价且单位单价 ≠ 0，阻止提交并标红提示
  - 对于外币交易，单位单价即为汇率（1 单位外币 = X HKD）
  - 若单位单价为 0，该笔交易不增加持仓单位，仅影响余额
- ✨ `Transaction` 模型新增 `trans_unit` / `trans_unit_price` 字段
- ✨ CSV 导入支持 `unit` / `单位`、`unit_price` / `单位单价` 列
- ✨ `AccountBalance.deposit_unit` 计算优先使用 `trans_unit`（有则忽略 `trans_fx_amount`）

### 变更
- 🔧 `_ensure_eod_balance()` 持仓成本计算：有 `trans_unit` / `trans_unit_price` 时使用新字段

---

## [0.3.6] - 2026-06-29

### 新增
- ✨ 资产负债表新增"操作"列，每账户显示"详情"按钮
  - 点击跳转至仪表盘，自动筛选该账户在选定日期范围内的所有交易
  - 仪表盘右上角显示"↩ 返回账户概览"按钮
- ✨ 账户详情视图导航逻辑
  - 从详情页点击"仪表盘"/"财务管家"导航链接 → 回到无账户筛选的交易概览，保留日期范围
  - 从详情页点击"账户概览"导航链接 → 与返回按钮行为一致

### 修复
- 🐛 修复从账户详情视图点击导航栏"仪表盘"后，账户筛选未能清除（session 回退逻辑导致）

---

## [0.3.5] - 2026-06-27

### 新增
- ✨ 外币账户明细展开显示 AccountBalance 字段（点击明细行 ▶ 展开）
  - 持仓单位 (`deposit_unit`)：期初 / 期末
  - 单位成本汇率 (`account_unit_cost_rate`)：期初 / 期末

### 变更
- 🔧 `account_balance` 表列重命名：`account_fx_cost_rate` → `account_unit_cost_rate`（适用范围更广）
- 🔧 数据库启动时自动迁移：检测旧列名执行 `ALTER TABLE RENAME COLUMN`

### 修复
- 🐛 修复 `toggleBsDetails` 因 `fx-detail` 行中断导致后续账户不显示
- 🐛 修复 FX 子行缺少 `<td>` 标签导致内容溢出表格

---

## [0.3.4] - 2026-06-27

### 新增
- ✨ 资产负债表交易核对状态图标（类别行 / 明细行 / 合计行）
  - ⚑ FLAGGED > ⚠ UNVERIFIED > ✓ VERIFIED
  - 图标随余额居中显示（双行 FX 余额垂直居中）
  - 类别行汇总子账户最差状态，合计行汇总全部账户最差状态
  - 无交易账户默认显示 ✓ VERIFIED
- ✨ 资产负债表资产/负债表格固定列宽（`table-layout: fixed`）

### 变更
- 🎨 外币账户余额为 0 时仍显示双行 FX 格式（`USD 0.00` + `HKD 0.00`）
- 🎨 外币账户余额判断条件从 `_has_fx_*` 改为 `item.currency != 'HKD'`
- 🎨 移除账户名称列中冗余的 FX 余额显示（已迁移至余额列）

### 修复
- 🐛 修复余额状态优先级错误（FLAGGED 应为最高优先级）
- 🐛 修复 `worst_status` 宏空白字符导致 status_badge 匹配失败

---

## [0.3.3] - 2026-06-27

### 新增
- ✨ 页面跳转时保留筛选条件（仪表盘 ↔ 账户概览）
  - 导航栏链接自动携带当前页面日期参数
  - Flask session 持久化所有筛选条件（日期、状态、分类、账户），离开后返回自动恢复

---

## [0.3.2] - 2026-06-27

### 新增
- ✨ 金额千分位逗号格式化（自定义 Jinja2 `amt` 过滤器，支持小数位数和符号）
- ✨ 账户余额外汇追踪：`get_account_eod_balance()` 自动汇总外汇交易，计算成本汇率
- ✨ 资产负债表外汇账户双行显示（外汇金额 + HKD 等值，与交易页面格式一致）
- ✨ 后端同时传递期初/期末外汇余额信息（`_has_fx_start` / `_has_fx_end`）

### 变更
- 🎨 资产负债表所有金额添加 `HKD` 前缀，与交易列表格式统一
- 🎨 资产金额使用 `text-income` 绿色加粗样式
- 🎨 负债金额使用 `text-expense` 红色加粗样式
- 🎨 流动资产/负债变动金额颜色统一使用 CSS 类（替代内联 style）
- 🎨 外汇账户信息去除"外汇："前缀，采用与交易页面一致的格式
- 🎨 账户概览页面布局更新

### 修复
- 🐛 修复 `_has_fx` / `_account_fx_amount` 仅引用期末余额的问题（拆分为期初/期末字段）
- 🐛 修复外汇账户 HKD 折算汇率方向错误

### 测试
- ✅ 新增 `test_import_transaction.py`（745 行测试代码）

---

## [0.3.1] - 2026-06-12

### 新增
- ✨ 交易批量删除功能（`/batch-delete`）
- ✨ 余额缓存失效机制（`invalidate_account_balances()`）：新增/删除/编辑交易时自动清理对应日期后的日终余额缓存

### 变更
- 🎨 仪表盘新增批量删除按钮

### 测试
- ✅ 新增批量删除测试（含空选择、转账配对删除等边界情况）

---

## [0.3.0] - 2026-06-12

### 新增
- ✨ 账户概览"财务状况"标签页 — 资产负债表
  - 按账户类型汇总的资产/负债分组视图（可展开明细）
  - 日期范围筛选
  - 总资产/总负债/净资产汇总卡片
  - 非 HKD 账户按各自日期汇率自动折算为 HKD
- ✨ `compute_balance_sheet()` 资产负债表计算函数
- ✨ `get_account_eod_balance()` 日终余额查询函数（含缓存表）
- ✨ `get_fx_rate_to_hkd()` 外汇汇率查询函数
- ✨ `AccountType.is_liability` 属性（区分资产/负债）

### 变更
- 🎨 账户概览页面拆分为"财务状况"和"账户管理"两个标签页
- 🎨 账户下拉框按 类型·机构·拥有者·名称(货币) 排序

### 数据库
- 🗄️ Account 模型新增 `account_balance` 缓存表、`currency_conversion` 汇率表

---

## [0.2.9] - 2026-06-10

### 新增
- ✨ 添加交易时支持选择货币（非 HKD 币种）

### 变更
- 🎨 仪表盘、账户概览、导入页面布局更新

---

## [0.2.8] - 2026-06-10

### 修复
- 🐛 修复导入手动匹配分类 bug：key 格式由 4 元组改为 5 元组 `(year, type, group, category, title)`
- 🐛 修复年份从交易日期提取（替代空字符串）
- 🐛 修复手动映射被已有自动映射覆盖的优先级问题
- 🐛 修复重复插入 `bluecoins_category_mapping` 导致的 IntegrityError
- 🐛 修复 `_match_owner()` 兜底逻辑：`return self.owner` 改为 `return None`（与文档一致）
- 🐛 修复创建新分类表单缺少别名字段
- 🐛 修复选择已有分类时仍创建新分类（`new_category_*` 未检查 `map_category_*` 是否为 `__new__`）
- 🐛 修复测试数据库隔离：`create_app()` 支持 `test_config` 参数，确保 engine 绑定临时 DB

### 变更
- 🔧 移除八达通交易分类特殊处理，所有交易统一 5 元组精确匹配

### 测试
- ✅ 测试用例从 104 增至 105，全部通过

---

## [0.2.7] - 2026-06-09

### 新增
- ✨ 数据管理页面（`/data`）：数据库备份/恢复/手动查询

---

## [0.2.6] - 2026-06-08

### 修复
- 🐛 修复交易导入预览中手动匹配账户功能

### 变更
- 🎨 导入页面布局优化

---

## [0.2.5] - 2026-06-07

### 变更
- 🎨 账户管理页面布局更新

---

## [0.2.4] - 2026-06-06

### 变更
- 🎨 导入页面布局更新（三列式布局，格式说明折叠面板）

---

## [0.2.3] - 2026-06-06

### 新增
- ✨ 分类导入 CSV 完整功能与测试
- ✨ 分类映射去重检测

### 测试
- ✅ 新增 `test_import_category.py`（13 个测试用例）

---

## [0.2.2] - 2026-06-05

### 新增
- ✨ 账户导入 CSV 完整功能与测试
- ✨ 账户映射去重检测（id/组合/空行/owner 匹配）

### 变更
- 🎨 账户页面布局优化（类型·机构·拥有者·名称排序）

### 测试
- ✅ 新增 `test_import_account.py`（17 个测试用例）

---

## [0.2.1] - 2026-05-06

### 修复
- 🐛 家庭管理页面过滤掉 `user_id=None` 的家庭共享 Owner
- 🐛 修复家庭页面成人计数显示错误

### 测试
- ✅ 新增 `test_family_page_excludes_family_owner` 测试用例

---

## [0.2.0] - 2026-05-06

### 新增
- ✨ Bluecoins 数据导入功能（账户/分类/交易 CSV 导入）
- ✨ 导入页面（`/import`），三步式导入流程
- ✨ Bluecoins 账户映射表（`bluecoins_account_mapping`）
- ✨ Bluecoins 分类映射表（`bluecoins_category_mapping`，全局五元组唯一）
- ✨ 导入账户 CSV：自动创建账户和映射，重复检测（id/组合/空行/owner匹配）
- ✨ 导入分类 CSV：自动创建分类和映射，重复检测（组合/空行）
- ✨ 导入交易 CSV：自动匹配账户和分类，转账配对识别，状态映射
- ✨ 导入预览及手动处理：未匹配项即时创建账户/分类（完整表单含别名/机构/类型/货币/日期/拥有者）
- ✨ 导入结果弹窗显示（替代 Flash 消息）
- ✨ 跳过交易可导出 CSV 下载
- ✨ `query_db.py` 数据库手动查询脚本
- ✨ 家庭共享 Owner（`user_id=None`），用于 Bluecoins 账户归属家庭

### 变更
- 🎨 账户拥有者显示区分个人/家庭共享
- 🎨 导航栏：家庭管理和数据导入移至用户名下拉菜单
- 🎨 删除账户/分类时检查关联交易，弹窗选择删除或迁移
- 🎨 账户/分类删除前自动清理 Bluecoins 映射记录

### 修复
- 🐛 修复 Bluecoins 导入日期解析（日期列包含完整时间）
- 🐛 修复 CSV `DictReader` 引号解析问题（`skipinitialspace=True`）
- 🐛 修复导入交易 owner 权限不匹配问题
- 🐛 修复删除账户时 `bluecoins_account_mapping` 外键约束错误

### 数据库
- 🗄️ 新增 `bluecoins_account_mapping` 表
- 🗄️ 新增 `bluecoins_category_mapping` 表（联合唯一约束）
- 🗄️ Account 表新增联合唯一约束（名称+类型+机构+货币+拥有者）
- 🗄️ Category 表新增联合唯一约束（名称+大类+子类+类型）

### 测试
- ✅ 新增 `test_import.py`（31 个测试用例，未全部通过）
- ⚠️ v0.2.0 导入功能存在已知问题，测试未完全通过

---

## [0.1.3] - 2026-05-05

### 变更
- 🎨 家庭管理移至用户名下拉菜单
- 🎨 导航栏新增数据导入入口

### 修复
- 🐛 修复家庭页面成人计数错误

### 测试
- ✅ 测试用例从 70 增至 71

---

## [0.1.2] - 2026-05-05

### 新增
- ✨ 仪表盘筛选区移至统计卡片上方
- ✨ 新增按分类和账户筛选交易
- ✨ 添加交易时时间精确到分钟
- ✨ "现在"按钮一键填入当前日期时间
- ✨ 交易编辑功能（弹窗编辑账户/分类/金额/日期时间/备注）
- ✨ 交易状态管理（核对/取消核对/标记/取消标记）
- ✨ 批量核对选中交易
- ✨ 操作后自动保持在交易列表标签页

### 变更
- 🎨 账户下拉框按 类型·机构·拥有者·名称(货币) 排序
- 🎨 分类下拉框按 大类›子类›名称 排序
- 🎨 按钮统一浅色调配色
- 🎨 日期默认值从月初改为今日

### 修复
- 🐛 修复操作后跳回添加交易页面
- 🐛 修复"现在"按钮只填时间不填日期
- 🐛 修复筛选按钮缩放后文字溢出

### 数据库
- 🗄️ Transaction 表新增 `trans_status` 字段

### 测试
- ✅ 测试用例从 53 增至 70

---

## [0.1.1] - 2026-05-04

### 新增
- ✨ 交易状态管理（未核对/已核对/有疑问/已对账）
- ✨ 数据库启动时自动检查表结构并迁移

### 修复
- 🐛 修复分类添加时 `CategoryType` 枚举 KeyError
- 🐛 修复测试污染开发数据库问题
- 🐛 消除 SQLAlchemy `Query.get()` 弃用警告

### 测试
- ✅ 测试与开发数据库完全隔离

---

## [0.1.0] - 2026-05-04

### 新增
- ✨ 完整的数据库模型设计（9 个表）
- ✨ 用户认证系统（注册/登录/登出）
- ✨ 家庭管理（成员 CRUD、角色权限、密码重置）
- ✨ 账户管理（CRUD、账户类型、创建/关闭日期）
- ✨ 分类管理（CRUD、类型/大类/子类/别名）
- ✨ 交易管理（收入/支出/转账、日期筛选）
- ✨ 转账双记录配对逻辑
- ✨ 成人/小孩权限隔离
- ✨ 仪表盘统计（总收入/支出/转账/净收入）
- ✨ 响应式 Web 界面

### 安全
- 🔒 密码 bcrypt 哈希、CSRF 保护、XSS 防护、SQL 注入防护

### 测试
- ✅ 53 个测试用例全部通过

---

## [0.0.1] - 2026-05-04

### 新增
- 🎉 项目初始化
- 📝 基础文档（README、CHANGELOG、CONTRIBUTING）
- 📄 MIT 许可证

---

## 版本说明

| 图标 | 含义 |
|------|------|
| ✨ | 新增功能 |
| 🎉 | 项目初始化 |
| 🔒 | 安全相关 |
| 🗄️ | 数据库相关 |
| 🎨 | 界面样式 |
| 🐛 | Bug 修复 |
| ✅ | 测试相关 |
| ⚠️ | 已知问题 |

---

**最后更新**：2026年8月27日